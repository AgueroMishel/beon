# beon

## Prerequisites
* `npm` installed

## Setup
1. Clone and go the repo
2. Execute `npm install cypress`
3. Execute `npm install`

## Execution
**Window/Debug Mode:** `npx cypress open`
**Headless Mode:** `npx cypress run`
